services:
  - type: web
    name: voice-cancellation-app
    env: python
    plan: free
    buildCommand: ./render-build.sh
    startCommand: cd backend && uvicorn server:app --host 0.0.0.0 --port 10000
    envVars:
      - key: PORT
        value: 10000
    autoDeploy: true
